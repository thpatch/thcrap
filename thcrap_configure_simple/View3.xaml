<UserControl x:Class="thcrap_configure_simple.View3"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:thcrap_configure_simple"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="15" VerticalAlignment="Center">
            Choose the patch you want
        </TextBlock>
        <StackPanel Grid.Row="1">
            <RadioButton FontSize="15" GroupName="patch"><TextBlock>lang_de</TextBlock></RadioButton>
            <RadioButton FontSize="15" GroupName="patch"><TextBlock>lang_en</TextBlock></RadioButton>
            <RadioButton FontSize="15" GroupName="patch"><TextBlock>lang_fr</TextBlock></RadioButton>
        </StackPanel>
        <Button Grid.Row="2" FontSize="15" Padding="8,2" HorizontalAlignment="Center" VerticalAlignment="Center" Click="next">Next</Button>
    </Grid>
</UserControl>
